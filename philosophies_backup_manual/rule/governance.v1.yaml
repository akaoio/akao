---
# Akao Philosophy: Rules can be toggled, not skipped
# All disabled rules tracked by GID with reason and audit trail

metadata:
  id: "akao:philosophy::rule/governance:v1"
  name: "Rules can be toggled, not skipped"
  version: "1.0.0"
  category: "rule"
  created_at: "2025-01-05T00:00:00Z"
  updated_at: "2025-01-05T00:00:00Z"
  author: "akao-framework"
  
description: |
  All disabled rules tracked by GID with reason and audit trail

formal_logic:
  domain: "rule"
  predicate: "governance_rule"
  axioms:
    - "Core axiom for Rules can be toggled, not skipped"
  invariants:
    - "Core invariant for Rules can be toggled, not skipped"
  
validation:
  conditions:
    - name: "check_governance_compliance"
      description: "Validates Rules can be toggled, not skipped compliance"
      logic: "elements.every(e => e.complies_with_governance_rules())"

implementation:
  enforcement_points:
    - "Core enforcement point"
    
  technical_requirements:
    - "Core technical requirement"
    
  user_interface:
    - "akao validate --governance"

measurement:
  metrics:
    - name: "governance_compliance_rate"
      description: "Percentage of elements following Rules can be toggled, not skipped rules"
      formula: "compliant_elements / total_elements * 100"

dependencies:
  requires: []
  recommends: []

audit:
  compliance_checks:
    - "Core compliance check for Rules can be toggled, not skipped"
    
  violation_severity: "MEDIUM"
  auto_fix_available: false
