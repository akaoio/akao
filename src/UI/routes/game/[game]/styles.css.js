import { css } from "/core/UI.js"

export const styles = css`
    :host {
        #meta {
            display: flex;
            flex-wrap: wrap;
            gap: var(--space);
        }
    }
`
export default styles
