import { css } from "/core/UI.js"

export const styles = css`
    :host {
        fieldset {
            margin: 0;
            padding: var(--space);
            border: var(--border);
        }
    }
`

export default styles
