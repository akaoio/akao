import { css } from "/core/UI.js"

export const styles = css`
    :host {
        width: 100%;
        display: flex;
        flex-direction: column;
        gap: var(--space);
    }
`

export default styles
