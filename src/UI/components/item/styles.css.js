import { css } from "/core/UI.js"

export const styles = css`
    :host {
        #pricing {
            display: flex;
            gap: var(--space);
            align-items: center;
            flex-direction: row;
        }
    }
`
export default styles
