# Nợ kỹ thuật: Lock / Order / Trade — Lỗi mật mã và thiết kế

**Phát hiện**: 2026-04-08

---

## ~~CRITICAL — `epub` vs `pub` trong `sea.secret()`~~ ✅ RESOLVED

GunDB SEA: `pair.pub` → Ed25519 (sign/verify), `pair.epub` → Curve25519 (secret/encrypt). White paper đã sửa 17 chỗ. `Lock/secret.js` dùng đúng `escrow.epub`. Lưu ý: truyền `pair` hay `epriv` vào arg thứ hai đều được — SEA tự tìm `epriv` trong pair.

---

## ~~CRITICAL — `get async method()` là syntax JavaScript không hợp lệ~~ ✅ RESOLVED

Lock.js đã được refactor thành `Lock/` subdir. `secret`, `address`, `index`, `unlock` đều là async methods thường, không còn getter.

---

## ~~CRITICAL — Constructor trả về object thay vì throw~~ ✅ RESOLVED

`Order`, `Trade`, và `Lock` giờ cùng follow một invariant chung: constructor hoặc tạo ra instance hợp lệ, hoặc `throw new Error("invalidInput")`. Việc localize error code được xử lý ở layer gọi phía trên, không bằng cách trả pseudo-instance từ constructor.

---

## ~~HIGH — TL và CL dùng cùng index~~ ✅ RESOLVED

`Lock/index.js` dùng `sha256(secret + ":" + this.type + ":" + this.trade)` — `type` là `"TL"` hoặc `"CL"` truyền vào constructor. White paper đã sửa toàn bộ.

---

## HIGH — xpub của recipient/affiliate phải lấy từ `gun.user(pub)` — ⚠️ PARTIAL

`Trade/deposit.js` có TODO comment. `gun.user(recipient.pub).get("xpub")` là bắt buộc — fetch từ path tùy ý là injection vector. **Chưa implement thực sự.**

---

## HIGH — xpub validation: recipient có thể write xpub crafted — 🔍 OPEN DESIGN QUESTION

Gun đảm bảo data trong `gun.user(pub)` được SEA-ký bởi chủ sở hữu. Nhưng chủ sở hữu có thể write **bất kỳ xpub nào** — kể cả xpub mà không ai có xprv. Funds bị khoá vĩnh viễn.

Các hướng đã cân nhắc:
- **Recipient tự ký**: `xprv_MP` ký `xpub_MP links to M.epub + P.epub` → self-satisfying, không chứng minh được gì.
- **Platform xác nhận**: Tập trung — vi phạm phi tập trung.

**Status**: Chưa có giải pháp. Cần nghiên cứu thêm.

---

## HIGH — `Trade/deposit.js` validate xpub yếu hơn `release()` / `refund()` — ⚠️ INTERNAL CONTRADICTION

Hiện tại:
- `deposit()` chỉ check `xpub` được publish trong `gun.user(pub)`
- `release()` và `refund()` lại check thêm `xpub === rootFromSecret(sea.secret(epub, platpair)).neuter().extendedKey`

Hệ quả: một `xpub` crafted có thể **pass ở bước deposit** nhưng **fail ở bước unhappy path** khi platform cần release/refund. Điều này tạo ra trạng thái nguy hiểm: tiền đã được gửi vào lock address nhưng platform không còn unlock theo invariant mà code sau này đòi hỏi.

**Nợ cần trả**:
- Đồng bộ invariant của `deposit()` với `release()` / `refund()`
- Hoặc tách rõ 2 mode và chấp nhận semantic hiện tại bằng design doc riêng

---

## ~~HIGH — `release()` / `refund()` đang ghi trạng thái hoàn tất nhưng chưa có on-chain settlement~~ ✅ RESOLVED

`Trade/release.js` và `Trade/refund.js` giờ persist đúng semantic capability state:
- `status: "release_ready"` khi platform đã resolve được release path/index
- `status: "refund_ready"` khi platform đã resolve được refund path/index

Điều này tránh hiểu nhầm rằng settlement đã xảy ra trên chain khi thực tế code mới chỉ resolve deterministic spend paths.

**Nợ còn lại**:
- implement settlement/reveal flow thực sự nếu muốn đi từ `*_ready` sang trạng thái hoàn tất trên chain

---

## ~~HIGH — `tradeId` chứa `Date.now()` — phải store, không recompute~~ ✅ RESOLVED

`Trade/id.js` cache `this._id`, `Order/match.js` trả về `{ tradeId, ts }` để caller store vào Gun. Không recompute.

---

## ~~MEDIUM — Principle of least privilege trong constructor~~ ✅ RESOLVED

Lock constructor: `new Lock({ payer, escrow, recipient, trade, type })` — chỉ nhận đúng thứ cần.

---

## ~~MEDIUM — `sha256()` custom chỉ đúng với ASCII input~~ ✅ RESOLVED

Note "All inputs must be ASCII-safe strings" đã có trong `Lock/secret.js`.

---

## LOW — Đụng độ naming: `pair.pub` (signing) vs `Wallet.public` (EVM address)

Hai khái niệm hoàn toàn khác nhau dùng tên gần nhau:
- `Access.pair.pub` → SEA Ed25519 signing key (Gun identity)
- `Wallet.public` (trong `src/core/Wallet.js`) → EVM public key (address)

Cần giữ consistent naming trong implementation.

---

---

## ~~CRITICAL — Trade record soul chưa được thiết kế~~ ✅ RESOLVED

**Giải pháp**: Mỗi bên ghi vào user namespace của mình, keyed by `tradeId`. Xem Section 6.3 white paper.

```javascript
gun.user(seller.pub).get("trades").get(tradeId).put({ delivered: true })
gun.user(buyer.pub).get("trades").get(tradeId).put({ confirmed: true, unlock_index_TL, unlock_index_CL })
```

---

## CRITICAL — `tradeId` dùng string concatenation không có separator

```js
// SAI — concatenation trực tiếp, có thể ambiguous
tradeId = sha256(orderId + M.pub + T.pub + timestamp)

// ĐÚNG — domain separator giống orderId
tradeId = sha256(orderId + ":" + M.pub + ":" + T.pub + ":" + timestamp)
```

**Đã sửa**: White paper cập nhật cả 3 chỗ.

---

## ~~HIGH — Order discovery cần migrate sang `SEA.pen({ params })` theo `baseId + side + candle`~~ ✅ RESOLVED

Sau khi nâng cấp `@akaoio/gun`, hướng chính quy là:

- `SEA.pen({ params: { baseId, side, candle } })` tạo soul riêng cho từng market window
- key chỉ còn: `<timestamp>:<pub>:<nonce>`
- ownership dùng **full pub**, không còn `pub8`

Shop đã migrate đồng bộ sang model mới và `Order.test.js` hiện có coverage cho discovery thật:

- chỉ discover đúng partition `{ baseId, side, candle }`
- scan đúng current + previous candle
- không leak order từ baseId/side khác hoặc candle cũ hơn window

**Đã trả xong**:
- `Order/soul.js` dùng `params = { baseId, side, candle }`
- `Order/key.js` dùng `<timestamp>:<pub>:<nonce>`
- `Order/create.js`, `Order/fetch.js`, `Order/cancel.js`, `Order/match.js` đã migrate
- tests/docs chính đã bỏ giả định `pub8` / global soul cũ

---

## ~~HIGH — Dynamic equality (`seg(pub) === R[5]`) mới giải quyết được owner binding ở Pen layer~~ ✅ RESOLVED

Gun repo giờ đã hỗ trợ `eq: [<expr>, <expr>]` và `penStage` cũng inject `R[5]` từ authenticated writer hoặc `opt.authenticator.pub`, nên order soul có thể ràng buộc full writer pub một cách thực thi được trong runtime thật.

---

## ~~HIGH — FP → TL/CL KHÔNG phải atomic transaction~~ ✅ RESOLVED (white paper)

White paper đã xóa từ "atomic", thêm cảnh báo rõ. `Trade/deposit.js` có comment cảnh báo sequential txs và TODO check receipts.

---

## HIGH — FP race condition (front-running) — ⚠️ MITIGATION PENDING

Maker có thể drain FP wallet sau khi Taker accept, trước khi deposit xảy ra. Thiết kế limitation không dùng smart contract. `Order/proof.js` cung cấp method `proof()` để Taker re-verify tại match time — **chưa được gọi thực sự trong `Order/match.js`**.

---

## ~~HIGH — Recipient có thể write xpub sai/crafted~~ → moved to OPEN DESIGN QUESTION above

---

## ~~MEDIUM — FP index thiếu domain separator~~ ✅ RESOLVED

`Order/proof.js` dùng `sha256("FP:" + orderId)`. White paper cập nhật.

---

## MEDIUM — Fate of commissionAmount khi không có affiliate — ❓ CHƯA QUYẾT ĐỊNH

Khi `referrer === null`, `commissionAmount` chưa được định nghĩa đi đâu. Các lựa chọn:
- Thêm vào `paymentAmount` (recipient nhận thêm)
- Chuyển vào `platformFee`
- Không charge commission từ đầu nếu không có affiliate

**Cần quyết định trước khi implement `Trade/deposit.js`**.

---

## MEDIUM — BIP-32 non-hardened child key leakage

BIP-32: `child_private_key + parent_xpub → parent_private_key`. Nếu spending key bị leak: attacker compromise toàn bộ HD tree. `Lock/unlock.js` có warning comment. **Rule khi implement**: wipe key khỏi memory ngay sau `sendTransaction()`, không log, không store.

---

## Tóm tắt những gì đã sửa trong white paper

| Vấn đề | Trạng thái |
|---|---|
| `sea.secret(P.pub)` → `sea.secret(P.epub)` — 17 chỗ | ✅ Đã sửa |
| TL/CL cùng index → domain separator `:TL:` / `:CL:` | ✅ Đã sửa |
| FP domain separator `"FP:"` | ✅ Đã sửa |
| `tradeId` separator `":"` | ✅ Đã sửa |
| Order soul overwrite → params + full pub equality | ✅ Hướng đã chốt |
| FP→TL/CL atomic → sequential warning | ✅ Đã sửa |

---

## Checklist implement (2026-04-09)

- [x] `Lock/` — `secret`, `index`, `address`, `unlock` theo pattern Wave.js
- [x] `Lock` constructor: `{ payer, escrow, recipient, trade, type }`
- [x] `Lock/index.js` domain separator `:TL:` / `:CL:`
- [x] `Order/` — `id`, `key`, `create`, `cancel`, `proof`, `match`, `fetch`
- [x] `Order/key.js` — Pen key format với PoW
- [x] `Order/proof.js` — FP balance verify với `"FP:"` domain separator
- [x] `Trade/` — `id`, `create`, `deposit`, `deliver`, `confirm`, `dispute`, `refund`, `release`, `read`
- [x] `Trade/id.js` — cached, không recompute
- [x] `Lock/secret.js` — ASCII-safe note
- [ ] `Trade/deposit.js` — xpub fetch qua `gun.user(pub)` (còn TODO)
- [ ] `Trade/deposit.js` — align xpub validation invariant với `release()` / `refund()`
- [ ] `Order/create.js` — migrate sang soul params `{ baseId, side, candle }`
- [ ] `Order/fetch.js` — query current/previous parameterized souls
- [ ] `Order/cancel.js` / `Order/match.js` — derive candle từ timestamp key và write vào đúng soul
- [ ] `Trade/deposit.js` — check từng tx receipt, recovery path (còn TODO)
- [ ] `Trade/release.js` / `Trade/refund.js` — settlement semantics: on-chain send hoặc rename state cho đúng
- [ ] `Order/match.js` — gọi `proof()` trước khi match (còn TODO)
- [ ] Quyết định fate of `commissionAmount` khi không có affiliate
