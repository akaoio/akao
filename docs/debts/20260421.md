# Technical Debt: Hardcoded chain→token standard map in wallets UI

**Discovered**: 2026-04-21  
**PR**: #60  
**Status**: Resolved

---

## Issue

`src/UI/routes/deposit/index.js` and `src/UI/routes/withdraw/index.js` each hardcoded a `TOKEN_STANDARD` map:

- `1`, `11155111` → `ERC20`
- `56`, `97` → `BEP20`

This is a brittle UI-level mapping that can drift from actual chain metadata.

## Resolution

- Removed `TOKEN_STANDARD` from both route files.
- `Logic.chains()` in `src/UI/components/wallets/logic.js` now derives `standard` from `chain.currencies[address].ABI` (filtered by selected currency name when provided) — data already loaded into `Statics.chains[id].contracts[address]` via `loadContract`.
- `deposit/index.js` uses `c.standard` from `Logic.chains()`.
- `withdraw/index.js` reads ABI inline from `chain.currencies` in `_syncNetworkInfo()`.

## Related Files

- `src/UI/components/wallets/logic.js`
- `src/UI/routes/deposit/index.js`
- `src/UI/routes/withdraw/index.js`
