– Phác thảo các lớp Order Trade Lock, chủ yếu stubs
– Tiếp tục hoàn thiện White paper, tăng cường bảo mật về mặt lý thuyết 
- Nâng cấp `SEA.pen()` ở gun repo một cách chính quy để hỗ trợ compile-time `params` generic, không monkey patch
- Refactor `Order` sang soul theo `{ baseId, side, candle }`
- Đổi key order sang `<timestamp>:<pub>:<nonce>` và bỏ toàn bộ giả định `pub8`
- Cập nhật test/docs đồng bộ với model order mới
