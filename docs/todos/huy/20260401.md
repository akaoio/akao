# To-Do — 2026 Apr 01

## 1. Game theming — `meta.yaml` color strategy

Decide whether each game's `meta.yaml` should declare `text_color` and/or `--game-primary`, and define the authoring workflow for those values.

**Open questions:**

- Do we support a curated set of pre-defined color schemes that can be assigned to games, or do we derive colors dynamically from each game's logo / hero assets?
- If derived: what extraction method — dominant color, vibrant swatch, or manual override?
- How do `text_color` and `--game-primary` interact with the existing light/dark/cyberpunk themes to ensure contrast and accessibility?

---

## 2. `#orbit` menu — z-index conflict with `.catalog-sticky`

The `#orbit` menu is currently being overlapped by `.catalog-sticky`, making it inaccessible when both are visible.

**Proposed resolution paths:**

- Adjust the z-index stacking order so `#orbit` always renders above `.catalog-sticky`.
- On mobile: collapse `.catalog-sticky` automatically when `#orbit` opens.
- On desktop: `.catalog-sticky` does not collapse in its current form — the fix must work without relying on that behaviour. Investigate a layout-level solution (e.g. stacking context isolation or a top-layer approach) that handles both viewports cleanly.

---

## 3. Brand glitch effect — production-ready iteration from `glitched_v0`

Evolve `glitched_v0` into the canonical brand glitch animation.

**Direction:**

- Refine timing, color palette, and distortion shapes so the effect reads as intentional brand identity rather than a generic glitch.
- Explore variants: RGB channel split, scanline tearing, block corruption — pick the one that best complements the cyberpunk design language.
- Define reuse guidelines: which surfaces use the glitch (logo, hero headings, loading states), at what trigger (hover, page load, idle loop), and with what reduced-motion fallback.
