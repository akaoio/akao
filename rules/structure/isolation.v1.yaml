---
# Rule: One class per folder enforcement
# Philosophy: akao:philosophy::structure:isolation:v1

metadata:
  id: "akao:rule::structure:isolation:v1"
  name: "One class per folder enforcement"
  version: "1.0.0"
  category: "structure"
  philosophy: "akao:philosophy::structure:isolation"
  created_at: "2025-01-05T00:00:00Z"

description: |
  Enforces the One class per folder philosophy.

validation_logic:
  conditions:
    - name: "check_structure_isolation_compliance"
      implementation: "validate_structure_isolation_compliance()"

test_cases:
  - name: "structure_isolation_compliance_pass"
    type: "compliance"
    expected: "validation_success"
    
  - name: "structure_isolation_violation_fail"
    type: "violation"
    expected: "validation_failure_with_gid"

dependencies:
  requires: []
  recommends: []

audit:
  violation_severity: "MEDIUM"
  auto_fix_available: false
