---
# Rule: No rules without tests enforcement
# Implements: akao:philosophy::testing/coverage:v1

metadata:
  gid: "akao:rule::testing/coverage:v1"
  name: "No rules without tests enforcement"
  version: "1.0.0"
  category: "testing"
  philosophy_gid: "akao:philosophy::testing/coverage:v1"
  created_at: "2025-01-05T00:00:00Z"

description: |
  Enforces the No rules without tests philosophy.

validation_logic:
  conditions:
    - name: "check_testing_coverage_compliance"
      implementation: "validate_testing_coverage_compliance()"

test_cases:
  - name: "testing_coverage_compliance_pass"
    type: "compliance"
    expected: "validation_success"
    
  - name: "testing_coverage_violation_fail"
    type: "violation"
    expected: "validation_failure_with_gid"

dependencies:
  requires: []
  recommends: []

audit:
  violation_severity: "MEDIUM"
  auto_fix_available: false
