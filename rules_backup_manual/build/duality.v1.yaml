---
# Rule: Everything must build dev plus prod enforcement
# Implements: akao:philosophy::build/duality:v1

metadata:
  id: "akao:rule::build/duality:v1"
  name: "Everything must build dev plus prod enforcement"
  version: "1.0.0"
  category: "build"
  philosophy: "build/duality"
  created_at: "2025-01-05T00:00:00Z"

description: |
  Enforces the Everything must build dev plus prod philosophy.

validation_logic:
  conditions:
    - name: "check_build_duality_compliance"
      implementation: "validate_build_duality_compliance()"

test_cases:
  - name: "build_duality_compliance_pass"
    type: "compliance"
    expected: "validation_success"
    
  - name: "build_duality_violation_fail"
    type: "violation"
    expected: "validation_failure_with_gid"

dependencies:
  requires: []
  recommends: []

audit:
  violation_severity: "MEDIUM"
  auto_fix_available: false
